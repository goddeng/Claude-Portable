dist/
build/
*.zip
*.tar.gz

# Credentials & secrets - NEVER commit
configs/credentials.env
configs/ss-config.json
configs/server.env
configs/client.env

# Database
server/license.db
server/license.db-wal
server/license.db-shm

# TLS — private key MUST never be committed. cert.pem is public but we keep
# the whole directory out of git as a safety belt; the server generates its
# own cert once and the SPKI fingerprint is what gets distributed to clients.
server/tls/

# Node modules
node_modules/

# Runtime data
data/
.credentials.json
.claude.json
.license.json
.ss_args
.heartbeat.json
.server.json

# OS files
.DS_Store
Thumbs.db
